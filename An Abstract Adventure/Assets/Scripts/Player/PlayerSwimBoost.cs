using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerSwimBoost : MonoBehaviour
{
/*    public float boostForce;
    public float boostDelay;

    private bool canBoost;
    private Rigidbody2D rb;
    private PlayerSwim playerSwim;

    void Awake()
    {
        canBoost = true;
        rb = GetComponent<Rigidbody2D>();
        playerSwim = GetComponent<PlayerSwim>();
    }

    public void SwimBoost()
    {
        if (canBoost && Input.GetKeyDown(KeyCode.Space))
        {
            rb.AddForce(playerSwim.swimDir * boostForce * 10, ForceMode2D.Impulse);
            StartCoroutine(WaitToBoost());
        }
    }

    IEnumerator WaitToBoost ()
    {
        canBoost = false;
        yield return new WaitForSeconds(boostDelay);
        canBoost = true;
    }*/
}
